(() => {
'use strict';
const $=id=>document.getElementById(id);
const schema={id:'제품코드 *',name:'제품명 *',catalogType:'상위 분류',catalogGroup:'일반식품 하위 분류',category:'원료 카테고리 *',subcategory:'세부 분류',marker:'지표성분',spec:'규격 / 함량',packaging:'포장단위',origin:'원산지',function:'주요 특성',application:'어플리케이션',form:'형태'};
let products=structuredClone(window.ECHO_PRODUCTS||[]),selected=-1,dirty=false,formDirty=false;
for(const [key,label] of Object.entries(schema)){const wrap=document.createElement('label');wrap.textContent=label;const input=document.createElement(['catalogType','catalogGroup'].includes(key)?'select':['function','application'].includes(key)?'textarea':'input');input.id='field-'+key;if(['catalogType','catalogGroup'].includes(key)){const values=key==='catalogType'?['','건강기능식품','일반식품','식품첨가물']:['','일반원료','기능성부원료'];values.forEach(v=>input.add(new Option(v||'미지정',v)));}input.required=['id','name','category'].includes(key);wrap.append(input);$('fields').append(wrap);}
function list(){const query=$('find').value.trim().toLowerCase();$('entries').replaceChildren();products.forEach((p,i)=>{if(!`${p.id} ${p.name}`.toLowerCase().includes(query))return;const b=document.createElement('button');b.type='button';b.textContent=`${p.id} · ${p.name}${p.visible===false?' (숨김)':''}`;b.className=selected===i?'selected':'';b.addEventListener('click',()=>{if(formDirty&&!confirm('현재 입력 중인 내용을 버리고 다른 제품을 열까요?'))return;choose(i);});$('entries').append(b);});}
function choose(i){selected=i;for(const key of Object.keys(schema))$('field-'+key).value=products[i]?.[key]||'';$('visible').checked=products[i]?.visible!==false;formDirty=false;list();}
function say(text){$('status').textContent=text;}
$('find').addEventListener('input',list);
$('edit').addEventListener('input',()=>formDirty=true);
$('edit').addEventListener('submit',e=>{e.preventDefault();const p=Object.fromEntries(Object.keys(schema).map(k=>[k,$('field-'+k).value.trim()]));if(!p.id||!p.name||!p.category){say('제품코드, 제품명, 카테고리를 입력해 주세요.');return;}if(products.some((x,i)=>x.id===p.id&&i!==selected)){say('같은 제품코드가 있습니다. 다른 코드를 사용해 주세요.');return;}p.catalogGroup=p.catalogType==='일반식품'?(p.catalogGroup||'일반원료'):'';p.visible=$('visible').checked;if(selected<0){products.push(p);selected=products.length-1;}else products[selected]=p;dirty=true;formDirty=false;list();say('목록에 반영했습니다. products.js를 다운로드하여 data 폴더의 파일과 교체하세요.');});
$('add').addEventListener('click',()=>{if(formDirty&&!confirm('현재 입력 중인 내용을 버리고 새 제품을 추가할까요?'))return;choose(-1);$('field-id').focus();say('새 제품 정보를 입력한 뒤 목록에 반영해 주세요.');});
$('remove').addEventListener('click',()=>{if(selected<0)return;if(confirm(`“${products[selected].name}” 제품을 삭제할까요?`)){products.splice(selected,1);dirty=true;choose(-1);say('목록에서 삭제했습니다. 데이터 파일을 다운로드하면 삭제 내용도 포함됩니다.');}});
function download(name,content,type){const url=URL.createObjectURL(new Blob([content],{type}));const a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}
function ready(){if(formDirty){say('입력 중인 변경사항이 있습니다. 먼저 목록에 반영해 주세요.');return false;}return true;}
$('download').addEventListener('click',()=>{if(!ready())return;download('products.js','// Public catalog data\nwindow.ECHO_PRODUCTS = '+JSON.stringify(products,null,2)+';\n','text/javascript;charset=utf-8');dirty=false;say('products.js를 다운로드했습니다. GitHub의 data/products.js를 교체해 주세요.');});
$('backup').addEventListener('click',()=>{if(!ready())return;download('echo-products-backup.json',JSON.stringify(products,null,2),'application/json');say('JSON 백업을 다운로드했습니다. 고객 화면 반영용 파일은 products.js입니다.');});
$('import').addEventListener('change',async e=>{const file=e.target.files[0];if(!file)return;try{const rows=JSON.parse(await file.text());const ids=new Set();if(!Array.isArray(rows))throw Error();const safe=rows.map(p=>{if(!p||typeof p!=='object'||Array.isArray(p))throw Error();for(const key of Object.keys(schema))if(p[key]!==undefined&&typeof p[key]!=='string')throw Error();if(!p.id?.trim()||!p.name?.trim()||!p.category?.trim()||ids.has(p.id.trim()))throw Error();if(p.catalogType&&!['건강기능식품','일반식품','식품첨가물'].includes(p.catalogType))throw Error();if(p.catalogGroup&&!['일반원료','기능성부원료'].includes(p.catalogGroup))throw Error();ids.add(p.id.trim());return {...Object.fromEntries(Object.keys(schema).map(k=>[k,(p[k]||'').trim()])),visible:p.visible!==false};});if(!confirm(`현재 편집 목록을 ${safe.length}개 제품으로 교체할까요?`))return;products=safe;dirty=true;choose(-1);say(`${products.length}개 제품을 불러왔습니다. products.js를 다운로드해 주세요.`);}catch{say('파일을 읽지 못했습니다. 편집기에서 내려받은 JSON 백업과 중복되지 않는 제품코드를 확인해 주세요.');}finally{e.target.value='';}});
window.addEventListener('beforeunload',e=>{if(dirty||formDirty){e.preventDefault();e.returnValue='';}});
choose(products.length?0:-1);
})();
